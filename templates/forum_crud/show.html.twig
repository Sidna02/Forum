{% extends 'base.html.twig' %}

{% block title %}Forum{% endblock %}

{% block body %}
    <h1>Forum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forum.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ forum.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forum_crud_index') }}">back to list</a>

    <a href="{{ path('app_forum_crud_edit', {'id': forum.id}) }}">edit</a>

    {{ include('forum_crud/_delete_form.html.twig') }}
{% endblock %}
